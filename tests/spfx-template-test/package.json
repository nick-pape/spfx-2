{
  "name": "@microsoft/spfx-template-test",
  "description": "Tests that templates match expected output",
  "version": "1.0.0",
  "private": true,
  "main": "lib/index.js",
  "license": "MIT",
  "engines": {
    "node": ">=22.14.0 <23.0.0 || >=24.5.0 <25.0.0"
  },
  "scripts": {
    "build": "heft build --clean && heft test",
    "_phase:build": "heft run --only build -- --clean",
    "_phase:test": "heft run --only test -- --clean"
  },
  "dependencies": {
    "@microsoft/spfx-cli": "workspace:*",
    "@microsoft/spfx-template-api": "workspace:*",
    "@rushstack/ts-command-line": "~5.3.3",
    "@rushstack/terminal": "~0.22.3",
    "ignore": "^5.3.0"
  },
  "devDependencies": {
    "@microsoft/spfx-cli-build-rig": "workspace:*",
    "@rushstack/heft": "^1.2.7",
    "eslint": "9.37.0"
  }
}
