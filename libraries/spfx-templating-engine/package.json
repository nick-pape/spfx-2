{
  "name": "@microsoft/spfx-templating-engine",
  "description": "Library for generating SPFx solutions",
  "version": "1.0.0",
  "private": true,
  "main": "lib/index.js",
  "license": "MIT",
  "engines": {
    "node": ">=22.14.0 <23.0.0 || >=24.5.0 <25.0.0"
  },
  "scripts": {
    "build": "heft build --clean",
    "_phase:build": "heft run --only build -- --clean",
    "_phase:test": "heft run --only test -- --clean"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@microsoft/spfx-cli-build-rig": "workspace:*",
    "@rushstack/heft": "^1.1.6",
    "eslint": "~9.25.1"
  }
}