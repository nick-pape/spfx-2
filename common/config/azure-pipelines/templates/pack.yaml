# Template: Pack publishable packages into tarballs
# Creates .tgz files for all packages with a versionPolicyName.

parameters:
  - name: RepoPath
    type: string
    default: '$(Build.SourcesDirectory)'

steps:
  - template: /common/config/azure-pipelines/templates/install-run-rush.yaml@self
    parameters:
      Arguments: >
        publish
        --publish
        --pack
        --include-all
        --release-folder $(Build.ArtifactStagingDirectory)/packages
      DisplayName: 'Rush Pack'
      RepoPath: ${{ parameters.RepoPath }}
